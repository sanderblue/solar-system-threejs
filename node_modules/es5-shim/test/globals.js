"use strict";

var test = require('tape');

test('parseInt', function (t) {
    t.test('accepts a radix', function (st) {
        for (var i = 2; i <= 36; ++i) {
            st.equal(parseInt('10', i), i, '"10" in base ' + i + ' should be "' + i + '"');
        }
        st.done();
    });

    t.test('defaults the radix to 10 when the number does not start with 0x or 0X', function (st) {
        ['01', '08', '10', '42'].forEach(function (str) {
            st.equal(parseInt(str), parseInt(str, 10), 'parseInt("' + str + '") assumes a radix of 10');
        });
        st.done();
    });

    t.test('
});

